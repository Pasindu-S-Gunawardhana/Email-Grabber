# Email-Grabber
A command‑line tool for extracting email addresses from a list of websites. It crawls each site, scans for email patterns, and shallowly follows links that are likely to contain contact information (e.g., Contact Us, About, Support). Results are saved in a simple text format for easy use.
